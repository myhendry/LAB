import { NextPage } from "next";

import { Layout } from "../../components/common";

interface IProps {}

const Cancelled: NextPage<IProps> = () => {
  return <Layout>Cancelled</Layout>;
};

export default Cancelled;
